<UserControl x:Class="DocCreator01.Views.TextPartsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:DocCreator01.ViewModel"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel,
                                             IsDesignTimeCreatable=True}">
    <UserControl.Resources>
        <!-- Подходит всем Button внутри данного контрола -->
        <Style x:Key="SmallToolButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Width"  Value="25"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </UserControl.Resources>
    <DockPanel Margin="7,0,7,7" LastChildFill="True">

        <!-- Горизонтальная лента кнопок, примкнутая кверху -->
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,7,0,5">
            
            <Button Content="+"  Style="{StaticResource SmallToolButtonStyle}" Command="{Binding AddTextPartCommand}"/>
 
            <Button Content="–"  Style="{StaticResource SmallToolButtonStyle}" Command="{Binding RemoveTextPartCommand}"/>

            <Button Content="&lt;" Style="{StaticResource SmallToolButtonStyle}" Command="{Binding MoveUpCommand}"/>

            <Button Content="&gt;" Style="{StaticResource SmallToolButtonStyle}" Command="{Binding MoveDownCommand}"/>
        </StackPanel>

        <!-- Список текстовых частей – заполняет всё, что осталось -->
        <ListBox ItemsSource="{Binding CurrentProject.ProjectData.TextParts}"
                 SelectedItem="{Binding SelectedTab.TextPart, Mode=TwoWay}"
                 DisplayMemberPath="Title"
                 Background="#B7DAF2"
                 BorderThickness="0"
                 Padding="4"/>
    </DockPanel>
</UserControl>
